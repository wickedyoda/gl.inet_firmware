	#!/bin/sh
	# check if we are on real system
	if [ -z "${IPKG_INSTROOT}" ]; then
		echo "Removing rc.d symlink for gl-sdk4-nas-utils"
		/etc/init.d/gl_nas_sys disable
		/etc/init.d/gl_nas_diskmanager disable
	fi
	echo 'removeing gl-sdk4-nas-utils'
	exit 0
