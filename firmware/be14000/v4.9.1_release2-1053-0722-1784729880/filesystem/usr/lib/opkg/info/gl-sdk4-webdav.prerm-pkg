	#!/bin/sh
	# check if we are on real system
	if [ -z "${IPKG_INSTROOT}" ]; then
		echo "Removing rc.d symlink for gl-sdk4-webdav"
		/etc/init.d/webdav_ser disable
	fi
	echo 'removeing gl-sdk4-webdav'
	exit 0
