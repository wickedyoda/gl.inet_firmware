# Firmware binaries - store large ones externally, but allow small partition binaries in repo
# Excluded: system.new.dat (545MB), boot.img (41MB), boot_kernel.bin (37MB), NON-HLOS (199-214MB each)

# Boot extraction (large binary blobs)
firmware/*/boot_extraction/_boot.img.extracted/

# Decompressed/generated files
kernel_decompressed
kernel_lzma.bin

# Secrets scan
gitleaks-results.json
security-report.json

# Scan artifacts
bandit-results.json
npm-audit.json
pip-audit.json

# Ghidra projects
ghidra_project/
ghidra-project/
__pycache__/
